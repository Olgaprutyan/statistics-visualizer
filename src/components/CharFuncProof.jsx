import TeX from './proof/TeX'
import CharacteristicFunctionLimit from './proof/CharacteristicFunctionLimit'
import CharFuncNearZero from './proof/CharFuncNearZero'
import QuadraticApproxZoom from './proof/QuadraticApproxZoom'

function CharFuncProof() {
  return (
    <div className="moments-proof">
      <p className="moments-lead">
        Докажем центральную предельную теорему с помощью
        характеристических функций. Это доказательство требует лишь
        конечной дисперсии — существование моментов высоких порядков не
        нужно.
      </p>

      <h3>Формулировка</h3>

      <p>
        Пусть <TeX>{'X_1, X_2, \\ldots'}</TeX> — независимые одинаково
        распределённые случайные величины с
      </p>

      <TeX block>
        {'\\mathbb{E}[X_i] = \\mu, \\qquad \\operatorname{Var}(X_i) = \\sigma^2, \\qquad 0 < \\sigma^2 < \\infty.'}
      </TeX>

      <p>Центральная предельная теорема утверждает, что</p>

      <TeX block>
        {'\\frac{X_1 + \\cdots + X_n - n\\mu}{\\sigma\\sqrt{n}} \\;\\xrightarrow{d}\\; N(0,1).'}
      </TeX>

      <p>Мы докажем это с помощью характеристических функций.</p>

      <div className="moments-note">
        <strong>Главная идея</strong> проста: характеристическая функция
        суммы равна произведению характеристических функций; после
        стандартизации каждый множитель оказывается близок к{' '}
        <TeX>{'1 - t^2/(2n)'}</TeX>; произведение <TeX>{'n'}</TeX> таких
        множителей стремится к <TeX>{'e^{-t^2/2}'}</TeX> — а это
        характеристическая функция стандартного нормального распределения.
      </div>

      <h3>1. Что такое характеристическая функция</h3>

      <p>
        Характеристической функцией случайной величины <TeX>{'X'}</TeX>{' '}
        называется функция
      </p>

      <TeX block>
        {'\\varphi_X(t) = \\mathbb{E}\\left[e^{itX}\\right], \\qquad t \\in \\mathbb{R},'}
      </TeX>

      <p>
        где <TeX>{'i'}</TeX> — мнимая единица:{' '}
        <TeX>{'i^2 = -1'}</TeX>. По формуле Эйлера
      </p>

      <TeX block>
        {'e^{itX} = \\cos(tX) + i\\sin(tX),'}
      </TeX>

      <p>поэтому</p>

      <TeX block>
        {'\\varphi_X(t) = \\mathbb{E}[\\cos(tX)] + i\\,\\mathbb{E}[\\sin(tX)].'}
      </TeX>

      <p>
        В отличие от производящей функции моментов, характеристическая
        функция существует для любой случайной величины: поскольку{' '}
        <TeX>{'|e^{itX}| = 1'}</TeX>, математическое ожидание всегда
        определено. Характеристическая функция кодирует распределение
        полностью: если две случайные величины имеют одинаковые
        характеристические функции для всех <TeX>{'t'}</TeX>, они имеют
        одинаковые распределения.
      </p>

      <h3>2. Почему характеристические функции удобны для сумм</h3>

      <p>
        Пусть <TeX>{'U'}</TeX> и <TeX>{'V'}</TeX> независимы. Тогда
      </p>

      <TeX block>
        {'\\varphi_{U+V}(t) = \\mathbb{E}\\left[e^{it(U+V)}\\right] = \\mathbb{E}\\left[e^{itU} e^{itV}\\right].'}
      </TeX>

      <p>Из независимости</p>

      <TeX block>
        {'\\mathbb{E}\\left[e^{itU} e^{itV}\\right] = \\mathbb{E}\\left[e^{itU}\\right] \\mathbb{E}\\left[e^{itV}\\right],'}
      </TeX>

      <p>поэтому</p>

      <TeX block>
        {'\\varphi_{U+V}(t) = \\varphi_U(t)\\,\\varphi_V(t).'}
      </TeX>

      <div className="moments-key">
        Характеристическая функция суммы независимых величин равна
        произведению их характеристических функций. Сложная операция
        сложения превращается в обычное умножение.
      </div>

      <p>
        Для суммы <TeX>{'n'}</TeX> независимых одинаково распределённых
        величин
      </p>

      <TeX block>
        {'\\varphi_{X_1 + \\cdots + X_n}(t) = \\left(\\varphi_X(t)\\right)^n.'}
      </TeX>

      <h3>3. Стандартизация</h3>

      <p>Определим стандартизированные случайные величины</p>

      <TeX block>
        {'Y_j = \\frac{X_j - \\mu}{\\sigma}.'}
      </TeX>

      <p>
        Тогда <TeX>{'\\mathbb{E}[Y_j] = 0'}</TeX> и{' '}
        <TeX>{'\\operatorname{Var}(Y_j) = 1'}</TeX>, откуда{' '}
        <TeX>{'\\mathbb{E}[Y_j^2] = 1'}</TeX>. Нормированную сумму можно
        записать как
      </p>

      <TeX block>
        {'S_n = \\frac{Y_1 + \\cdots + Y_n}{\\sqrt{n}} = \\frac{X_1 + \\cdots + X_n - n\\mu}{\\sigma\\sqrt{n}}.'}
      </TeX>

      <p>Достаточно доказать, что</p>

      <TeX block>
        {'S_n \\;\\xrightarrow{d}\\; N(0,1).'}
      </TeX>

      <h3>4. Характеристическая функция нормированной суммы</h3>

      <p>
        Обозначим характеристическую функцию одной стандартизированной
        величины через <TeX>{'\\varphi(t) = \\mathbb{E}[e^{itY_1}]'}</TeX>.
        Характеристическая функция <TeX>{'Y_j / \\sqrt{n}'}</TeX> равна
      </p>

      <TeX block>
        {'\\varphi_{Y_j/\\sqrt{n}}(t) = \\mathbb{E}\\left[e^{itY_j/\\sqrt{n}}\\right] = \\varphi\\!\\left(\\frac{t}{\\sqrt{n}}\\right).'}
      </TeX>

      <p>
        Из независимости <TeX>{'Y_1, \\ldots, Y_n'}</TeX>
      </p>

      <TeX block>
        {'\\varphi_{S_n}(t) = \\left[\\varphi\\!\\left(\\frac{t}{\\sqrt{n}}\\right)\\right]^n.'}
      </TeX>

      <p>
        Теперь вся задача сводится к исследованию поведения{' '}
        <TeX>{'\\varphi(u)'}</TeX> при малых <TeX>{'u'}</TeX>. Когда{' '}
        <TeX>{'n'}</TeX> велико, аргумент{' '}
        <TeX>{'t / \\sqrt{n}'}</TeX> мал — нам нужно понять, как
        характеристическая функция ведёт себя около нуля.
      </p>

      <CharFuncNearZero />

      <h3>5. Главный локальный результат</h3>

      <p>
        Мы докажем, что при <TeX>{'u \\to 0'}</TeX>
      </p>

      <TeX block>
        {'\\varphi(u) = 1 - \\frac{u^2}{2} + o(u^2).'}
      </TeX>

      <p>
        Это разложение отражает два свойства стандартизированной величины:{' '}
        <TeX>{'\\mathbb{E}[Y] = 0'}</TeX> и{' '}
        <TeX>{'\\mathbb{E}[Y^2] = 1'}</TeX>. В общем случае, если у величины
        существуют первые два момента, то около нуля
      </p>

      <TeX block>
        {'\\varphi_Y(u) = 1 + iu\\,\\mathbb{E}[Y] - \\frac{u^2}{2}\\,\\mathbb{E}[Y^2] + o(u^2),'}
      </TeX>

      <p>
        так что линейный член исчезает из-за{' '}
        <TeX>{'\\mathbb{E}[Y] = 0'}</TeX>, а коэффициент перед{' '}
        <TeX>{'u^2'}</TeX> становится <TeX>{'-1/2'}</TeX> из-за{' '}
        <TeX>{'\\mathbb{E}[Y^2] = 1'}</TeX>.
      </p>

      <p>
        Формально хочется просто подставить <TeX>{'x = Y'}</TeX> в
        разложение экспоненты{' '}
        <TeX>{'e^{iux} = 1 + iux - \\tfrac{u^2 x^2}{2} + o(u^2 x^2)'}</TeX>{' '}
        и усреднить. Но <TeX>{'Y'}</TeX> может принимать очень большие
        значения, поэтому перенести остаток под знак математического
        ожидания автоматически нельзя — нужно отдельно показать, что
        усреднённый остаток имеет порядок <TeX>{'o(u^2)'}</TeX>. Сделаем
        это строго.
      </p>

      <h3>6. Строгое доказательство разложения около нуля</h3>

      <p>Рассмотрим функцию</p>

      <TeX block>
        {'r(x) = e^{ix} - 1 - ix + \\frac{x^2}{2}.'}
      </TeX>

      <p>
        По разложению экспоненты <TeX>{'r(x) = o(x^2)'}</TeX> при{' '}
        <TeX>{'x \\to 0'}</TeX>. Подставим <TeX>{'x = uY'}</TeX>:
      </p>

      <TeX block>
        {'e^{iuY} = 1 + iuY - \\frac{u^2 Y^2}{2} + r(uY).'}
      </TeX>

      <p>Взяв математическое ожидание и используя{' '}
        <TeX>{'\\mathbb{E}[Y] = 0'}</TeX>,{' '}
        <TeX>{'\\mathbb{E}[Y^2] = 1'}</TeX>,
      </p>

      <TeX block>
        {'\\varphi(u) = 1 - \\frac{u^2}{2} + \\mathbb{E}[r(uY)].'}
      </TeX>

      <p>
        Остаётся показать, что{' '}
        <TeX>{'\\mathbb{E}[r(uY)] = o(u^2)'}</TeX>. Запишем
      </p>

      <TeX block>
        {'\\frac{r(uY)}{u^2} = Y^2 \\cdot \\frac{r(uY)}{(uY)^2}.'}
      </TeX>

      <p>
        Для каждого фиксированного <TeX>{'Y'}</TeX> при{' '}
        <TeX>{'u \\to 0'}</TeX> дробь{' '}
        <TeX>{'r(uY)/(uY)^2 \\to 0'}</TeX>, поэтому{' '}
        <TeX>{'r(uY)/u^2 \\to 0'}</TeX> почти наверное.
      </p>

      <p>
        Для интегрируемой верхней оценки: для любого <TeX>{'x'}</TeX>
      </p>

      <TeX block>
        {'|e^{ix} - 1 - ix| \\le \\frac{x^2}{2},'}
      </TeX>

      <p>откуда</p>

      <TeX block>
        {'|r(x)| \\le |e^{ix} - 1 - ix| + \\frac{x^2}{2} \\le x^2,'}
      </TeX>

      <p>
        и поэтому <TeX>{'|r(uY)/u^2| \\le Y^2'}</TeX>. Но{' '}
        <TeX>{'\\mathbb{E}[Y^2] = 1 < \\infty'}</TeX>. По теореме Лебега
        о мажорируемой сходимости
      </p>

      <TeX block>
        {'\\frac{\\mathbb{E}[r(uY)]}{u^2} \\to 0.'}
      </TeX>

      <p>Итак, мы строго получили</p>

      <div className="moments-final">
        <TeX block>
          {'\\boxed{\\;\\varphi(u) = 1 - \\frac{u^2}{2} + o(u^2) \\quad \\text{при } u \\to 0\\;}'}
        </TeX>
      </div>

      <div className="moments-key">
        Для этого шага потребовался только конечный второй момент.
        Существование третьего, четвёртого или всех последующих моментов
        не требуется.
      </div>

      <QuadraticApproxZoom />

      <h3>7. Подставляем масштаб</h3>

      <p>
        Вернёмся к характеристической функции нормированной суммы:
      </p>

      <TeX block>
        {'\\varphi_{S_n}(t) = \\left[\\varphi\\!\\left(\\frac{t}{\\sqrt{n}}\\right)\\right]^n.'}
      </TeX>

      <p>
        Подставим <TeX>{'u = t / \\sqrt{n}'}</TeX>, тогда{' '}
        <TeX>{'u^2 = t^2 / n'}</TeX>:
      </p>

      <TeX block>
        {'\\varphi\\!\\left(\\frac{t}{\\sqrt{n}}\\right) = 1 - \\frac{t^2}{2n} + o\\!\\left(\\frac{1}{n}\\right).'}
      </TeX>

      <p>Значит,</p>

      <TeX block>
        {'\\varphi_{S_n}(t) = \\left[1 - \\frac{t^2}{2n} + o\\!\\left(\\frac{1}{n}\\right)\\right]^n.'}
      </TeX>

      <p>
        Это выражение напоминает классический предел{' '}
        <TeX>{'(1 + x/n)^n \\to e^x'}</TeX> с{' '}
        <TeX>{'x = -t^2/2'}</TeX>. Поэтому естественно ожидать, что{' '}
        <TeX>{'\\varphi_{S_n}(t) \\to e^{-t^2/2}'}</TeX>.
      </p>

      <CharacteristicFunctionLimit />

      <h3>8. Переход к экспоненте</h3>

      <p>
        При увеличении <TeX>{'n'}</TeX> аргумент{' '}
        <TeX>{'t / \\sqrt{n}'}</TeX> приближается к нулю. Поэтому
        каждый множитель определяется локальным квадратичным поведением
        характеристической функции. Отклонение одного множителя от
        единицы имеет порядок <TeX>{'1/n'}</TeX>, а таких множителей{' '}
        <TeX>{'n'}</TeX>. В результате малые изменения накапливаются и
        дают предел <TeX>{'e^{-t^2/2}'}</TeX>.
      </p>

      <p>Докажем переход аккуратно. Из</p>

      <TeX block>
        {'\\varphi\\!\\left(\\frac{t}{\\sqrt{n}}\\right) = 1 - \\frac{t^2}{2n} + o\\!\\left(\\frac{1}{n}\\right)'}
      </TeX>

      <p>
        следует, что существует последовательность{' '}
        <TeX>{'\\varepsilon_n(t)'}</TeX>, для которой{' '}
        <TeX>{'\\varepsilon_n(t) \\to 0'}</TeX> и
      </p>

      <TeX block>
        {'\\varphi\\!\\left(\\frac{t}{\\sqrt{n}}\\right) = 1 + \\frac{-t^2/2 + \\varepsilon_n(t)}{n}.'}
      </TeX>

      <p>Поэтому</p>

      <TeX block>
        {'\\varphi_{S_n}(t) = \\left[1 + \\frac{-t^2/2 + \\varepsilon_n(t)}{n}\\right]^n.'}
      </TeX>

      <p>
        Используем общий предел: если <TeX>{'a_n \\to a'}</TeX>, то{' '}
        <TeX>{'(1 + a_n / n)^n \\to e^a'}</TeX>. Здесь
      </p>

      <TeX block>
        {'a_n = -\\frac{t^2}{2} + \\varepsilon_n(t) \\;\\longrightarrow\\; -\\frac{t^2}{2}.'}
      </TeX>

      <p>Следовательно,</p>

      <div className="moments-final">
        <TeX block>
          {'\\boxed{\\;\\varphi_{S_n}(t) \\longrightarrow e^{-t^2/2}\\;}'}
        </TeX>
      </div>

      <h3>
        9. Почему <TeX>{'e^{-t^2/2}'}</TeX> соответствует нормальному
        распределению
      </h3>

      <p>
        Покажем, что <TeX>{'e^{-t^2/2}'}</TeX> является
        характеристической функцией{' '}
        <TeX>{'Z \\sim N(0,1)'}</TeX>. Плотность{' '}
        <TeX>{'Z'}</TeX> равна
      </p>

      <TeX block>
        {'f_Z(x) = \\frac{1}{\\sqrt{2\\pi}}\\,e^{-x^2/2}.'}
      </TeX>

      <p>Рассмотрим функцию</p>

      <TeX block>
        {'g(t) = \\varphi_Z(t) = \\frac{1}{\\sqrt{2\\pi}} \\int_{-\\infty}^{\\infty} e^{itx}\\,e^{-x^2/2}\\,dx.'}
      </TeX>

      <p>
        Дифференцируя по <TeX>{'t'}</TeX> и используя{' '}
        <TeX>{'x\\,e^{-x^2/2} = -\\frac{d}{dx} e^{-x^2/2}'}</TeX>,
        после интегрирования по частям получаем
      </p>

      <TeX block>{"g'(t) = -t\\,g(t)."}</TeX>

      <p>
        Также <TeX>{'g(0) = 1'}</TeX>. Решение этого
        дифференциального уравнения —{' '}
        <TeX>{'g(t) = e^{-t^2/2}'}</TeX>.
      </p>

      <div className="moments-final">
        <TeX block>
          {'\\boxed{\\;\\varphi_Z(t) = e^{-t^2/2}\\;}'}
        </TeX>
      </div>

      <h3>10. Теорема непрерывности Леви</h3>

      <p>Используем теорему непрерывности Леви:</p>

      <div className="moments-note">
        Если характеристические функции <TeX>{'W_n'}</TeX> сходятся
        поточечно к функции <TeX>{'\\varphi'}</TeX>, причём{' '}
        <TeX>{'\\varphi'}</TeX> является характеристической функцией
        некоторой случайной величины <TeX>{'W'}</TeX> и непрерывна в
        нуле, то <TeX>{'W_n \\xrightarrow{d} W'}</TeX>.
      </div>

      <p>
        Мы доказали, что для каждого <TeX>{'t'}</TeX>
      </p>

      <TeX block>
        {'\\varphi_{S_n}(t) \\to e^{-t^2/2}.'}
      </TeX>

      <p>
        Функция <TeX>{'e^{-t^2/2}'}</TeX> — характеристическая функция{' '}
        <TeX>{'Z \\sim N(0,1)'}</TeX>, непрерывная в нуле. По теореме
        Леви
      </p>

      <TeX block>
        {'S_n \\;\\xrightarrow{d}\\; Z.'}
      </TeX>

      <p>Возвращаясь к исходным случайным величинам,</p>

      <div className="moments-final">
        <TeX block>
          {'\\boxed{\\;\\frac{X_1 + \\cdots + X_n - n\\mu}{\\sigma\\sqrt{n}} \\;\\xrightarrow{d}\\; N(0,1)\\;}'}
        </TeX>
      </div>

      <p>
        Или, через выборочное среднее,
      </p>

      <div className="moments-final">
        <TeX block>
          {'\\boxed{\\;\\frac{\\sqrt{n}\\,(\\overline{X}_n - \\mu)}{\\sigma} \\;\\xrightarrow{d}\\; N(0,1)\\;}'}
        </TeX>
      </div>

      <p>Центральная предельная теорема доказана.</p>

      <h3>Почему это доказательство работает</h3>

      <p>
        После стандартизации каждая случайная величина имеет{' '}
        <TeX>{'\\mathbb{E}[Y] = 0'}</TeX> и{' '}
        <TeX>{'\\mathbb{E}[Y^2] = 1'}</TeX>. Её характеристическая
        функция около нуля выглядит как
      </p>

      <TeX block>
        {'\\varphi(u) = 1 - \\frac{u^2}{2} + o(u^2).'}
      </TeX>

      <p>
        Линейный член отсутствует из-за нулевого ожидания.
        Квадратичный член определяется дисперсией. В нормированной сумме
        каждая величина входит с коэффициентом{' '}
        <TeX>{'1/\\sqrt{n}'}</TeX>, поэтому возникает аргумент{' '}
        <TeX>{'t / \\sqrt{n}'}</TeX>. Для каждого слагаемого
        характеристическая функция приблизительно равна{' '}
        <TeX>{'1 - t^2/(2n)'}</TeX>. Перемножая <TeX>{'n'}</TeX> таких
        множителей, получаем
      </p>

      <TeX block>
        {'\\left(1 - \\frac{t^2}{2n}\\right)^{\\!n} \\longrightarrow e^{-t^2/2}.'}
      </TeX>

      <div className="moments-key">
        Нормальность появляется из сочетания двух фактов: каждое
        нормированное слагаемое даёт очень маленькое квадратичное
        изменение, а большое число этих маленьких изменений накапливается
        через произведение.
      </div>

      <h3>Почему исчезает информация о форме исходного распределения</h3>

      <p>
        После центрирования (<TeX>{'\\mathbb{E}[Y] = 0'}</TeX>) линейный
        член исчезает. После стандартизации ({' '}
        <TeX>{'\\mathbb{E}[Y^2] = 1'}</TeX>) квадратичный член становится
        одинаковым для всех распределений:{' '}
        <TeX>{'-u^2/2'}</TeX>.
      </p>

      <p>
        Члены третьего и более высоких порядков при подстановке{' '}
        <TeX>{'u = t/\\sqrt{n}'}</TeX> имеют масштаб{' '}
        <TeX>{'t^3 / n^{3/2}'}</TeX>. После умножения на{' '}
        <TeX>{'n'}</TeX> остаётся величина порядка{' '}
        <TeX>{'1/\\sqrt{n} \\to 0'}</TeX>. В пределе сохраняется
        информация только о первых двух моментах: математическом ожидании
        и дисперсии.
      </p>

      <h3>Роль условий теоремы</h3>

      <p>
        <strong>Независимость</strong> используется в ключевом равенстве{' '}
        <TeX>{'\\varphi_{Y_1 + \\cdots + Y_n}(t) = \\prod_{j=1}^{n} \\varphi_{Y_j}(t)'}</TeX>.
        Без неё математическое ожидание произведения нельзя разложить в
        произведение ожиданий.
      </p>

      <p>
        <strong>Одинаковое распределение</strong> позволяет записать{' '}
        <TeX>{'\\varphi_{S_n}(t) = [\\varphi(t/\\sqrt{n})]^n'}</TeX> — иначе
        вместо степени возникает произведение разных функций.
      </p>

      <p>
        <strong>Конечная дисперсия</strong> нужна для локального
        разложения <TeX>{'\\varphi(u) = 1 - u^2/2 + o(u^2)'}</TeX>.
        Именно второй момент определяет квадратичное поведение около
        нуля. Если дисперсия бесконечна, предельное распределение может
        оказаться не нормальным.
      </p>

      <h3>Сравнение с доказательством через моменты</h3>

      <p>
        Доказательство через моменты изучает{' '}
        <TeX>{'\\mathbb{E}[S_n^r]'}</TeX> для целых <TeX>{'r'}</TeX> и
        показывает, что они сходятся к моментам нормального распределения.
        Оно наглядно показывает, почему в пределе остаются парные
        совпадения индексов, но обычно требует существования моментов
        высоких порядков.
      </p>

      <p>
        Доказательство через характеристические функции работает при
        стандартных условиях классической ЦПТ:{' '}
        <TeX>{'\\mathbb{E}[X^2] < \\infty'}</TeX>. Вместо исследования
        каждого момента отдельно мы исследуем одну функцию{' '}
        <TeX>{'\\varphi_X(t) = \\mathbb{E}[e^{itX}]'}</TeX>, которая
        кодирует всё распределение. Поэтому это доказательство является
        более общим и обычно считается стандартным строгим доказательством
        классической ЦПТ.
      </p>
    </div>
  )
}

export default CharFuncProof
