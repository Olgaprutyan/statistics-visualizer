import TeX from './proof/TeX'
import LindebergConditionVisualizer from './proof/LindebergConditionVisualizer'
import RemainderControl from './proof/RemainderControl'

function LindebergProof() {
  return (
    <div className="moments-proof">
      <p className="moments-lead">
        Классическая ЦПТ рассматривает одинаково распределённые величины.
        Но одинаковое распределение не главное. Гораздо важнее, чтобы ни
        одно отдельное слагаемое не вносило заметную долю в случайные
        колебания всей суммы. Именно это формализует условие Линдеберга.
      </p>

      <h3>1. Постановка задачи</h3>

      <p>
        Для каждого <TeX>{'n'}</TeX> рассмотрим независимые случайные
        величины <TeX>{'X_{n,1}, \\ldots, X_{n,k_n}'}</TeX>. Такая
        структура называется <strong>треугольной схемой</strong>:
        распределения могут отличаться и внутри строки, и между строками.
        Предположим, что
      </p>

      <TeX block>
        {'\\mathbb{E}[X_{n,k}] = 0, \\qquad \\operatorname{Var}(X_{n,k}) = \\sigma_{n,k}^2 < \\infty.'}
      </TeX>

      <p>Обозначим сумму дисперсий в строке через</p>

      <TeX block>
        {'s_n^2 = \\sum_{k=1}^{k_n} \\sigma_{n,k}^2.'}
      </TeX>

      <p>Нас интересует нормированная сумма</p>

      <TeX block>
        {'\\frac{X_{n,1} + \\cdots + X_{n,k_n}}{s_n}.'}
      </TeX>

      <h3>2. Условие Линдеберга</h3>

      <p>
        Для любого <TeX>{'\\varepsilon > 0'}</TeX> потребуем, чтобы
      </p>

      <TeX block>
        {'\\frac{1}{s_n^2} \\sum_{k=1}^{k_n} \\mathbb{E}\\left[X_{n,k}^2\\,\\mathbf{1}\\{|X_{n,k}| > \\varepsilon s_n\\}\\right] \\longrightarrow 0.'}
      </TeX>

      <p>
        Индикатор{' '}
        <TeX>{'\\mathbf{1}\\{|X_{n,k}| > \\varepsilon s_n\\}'}</TeX>{' '}
        равен единице, когда величина оказывается большой по сравнению со
        стандартным отклонением всей суммы. Поэтому выражение под суммой
        измеряет вклад крупных значений <TeX>{'X_{n,k}'}</TeX> в её
        дисперсию, а после деления на <TeX>{'s_n^2'}</TeX> — долю общей
        дисперсии, связанную с крупными скачками.
      </p>

      <div className="moments-key">
        Условие Линдеберга говорит, что при увеличении <TeX>{'n'}</TeX>{' '}
        крупные отдельные скачки должны давать исчезающе малую долю общей
        дисперсии суммы.
      </div>

      <LindebergConditionVisualizer />

      <p>
        Условие Линдеберга не запрещает отдельным случайным величинам
        иногда принимать большие значения. Оно требует, чтобы суммарный
        вклад таких значений в дисперсию нормированной суммы стремился к
        нулю. Тогда общая случайность создаётся большим числом малых
        независимых воздействий, а не несколькими доминирующими скачками.
      </p>

      <p>
        В строгом доказательстве это условие позволяет контролировать
        остаток квадратичного разложения каждой характеристической
        функции.
      </p>

      <h3>3. Формулировка теоремы</h3>

      <p>
        Пусть для каждого <TeX>{'n'}</TeX> величины{' '}
        <TeX>{'X_{n,1}, \\ldots, X_{n,k_n}'}</TeX> независимы,{' '}
        <TeX>{'\\mathbb{E}[X_{n,k}] = 0'}</TeX>, а{' '}
        <TeX>{'s_n^2 = \\sum_k \\operatorname{Var}(X_{n,k}) > 0'}</TeX>.
        Если для любого <TeX>{'\\varepsilon > 0'}</TeX> выполняется
        условие Линдеберга, то
      </p>

      <div className="moments-final">
        <TeX block>
          {'\\boxed{\\;\\frac{X_{n,1} + \\cdots + X_{n,k_n}}{s_n} \\;\\overset{d}{\\longrightarrow}\\; N(0,1)\\;}'}
        </TeX>
      </div>

      <h3>4. Нормировка</h3>

      <p>Определим</p>

      <TeX block>
        {'Y_{n,k} = \\frac{X_{n,k}}{s_n}, \\qquad v_{n,k} = \\operatorname{Var}(Y_{n,k}) = \\frac{\\sigma_{n,k}^2}{s_n^2}.'}
      </TeX>

      <p>
        Здесь <TeX>{'v_{n,k}'}</TeX> — дисперсия нормированного слагаемого{' '}
        <TeX>{'Y_{n,k}'}</TeX>: она показывает, какую долю общей дисперсии
        суммы несёт <TeX>{'k'}</TeX>-е слагаемое (все такие доли в сумме
        дают единицу).
      </p>

      <p>
        Тогда <TeX>{'\\mathbb{E}[Y_{n,k}] = 0'}</TeX>, а сумма дисперсий в
        каждой строке равна единице:
      </p>

      <TeX block>
        {'\\sum_{k=1}^{k_n} v_{n,k} = \\frac{1}{s_n^2} \\sum_{k=1}^{k_n} \\sigma_{n,k}^2 = 1.'}
      </TeX>

      <p>Условие Линдеберга принимает вид</p>

      <TeX block>
        {'\\sum_{k=1}^{k_n} \\mathbb{E}\\left[Y_{n,k}^2\\,\\mathbf{1}\\{|Y_{n,k}| > \\varepsilon\\}\\right] \\longrightarrow 0,'}
      </TeX>

      <p>
        и нужно доказать, что{' '}
        <TeX>{'S_n = \\sum_k Y_{n,k} \\overset{d}{\\longrightarrow} N(0,1)'}</TeX>.
      </p>

      <h3>5. План доказательства</h3>

      <p>
        Обозначим характеристическую функцию{' '}
        <TeX>{'Y_{n,k}'}</TeX> через{' '}
        <TeX>{'\\varphi_{n,k}(t) = \\mathbb{E}[e^{itY_{n,k}}]'}</TeX>. Из
        независимости внутри строки
      </p>

      <TeX block>
        {'\\varphi_{S_n}(t) = \\prod_{k=1}^{k_n} \\varphi_{n,k}(t).'}
      </TeX>

      <p>
        Мы покажем, что каждый множитель близок к{' '}
        <TeX>{'1 - t^2 v_{n,k} / 2'}</TeX>, суммарный остаток исчезает, а
        так как <TeX>{'\\sum_k v_{n,k} = 1'}</TeX>, произведение стремится
        к <TeX>{'e^{-t^2/2}'}</TeX>.
      </p>

      <h3>6. Первое следствие: условие Феллера</h3>

      <p>
        Сначала покажем, что условие Линдеберга запрещает отдельному
        слагаемому иметь заметную дисперсию:{' '}
        <TeX>{'\\max_k v_{n,k} \\to 0'}</TeX>. Разобьём{' '}
        <TeX>{'v_{n,k} = \\mathbb{E}[Y_{n,k}^2]'}</TeX> на две части:
      </p>

      <TeX block>
        {'v_{n,k} = \\mathbb{E}\\left[Y_{n,k}^2 \\mathbf{1}\\{|Y_{n,k}| \\le \\varepsilon\\}\\right] + \\mathbb{E}\\left[Y_{n,k}^2 \\mathbf{1}\\{|Y_{n,k}| > \\varepsilon\\}\\right].'}
      </TeX>

      <p>
        На событии <TeX>{'|Y_{n,k}| \\le \\varepsilon'}</TeX> имеем{' '}
        <TeX>{'Y_{n,k}^2 \\le \\varepsilon^2'}</TeX>, поэтому
      </p>

      <TeX block>
        {'\\max_k v_{n,k} \\le \\varepsilon^2 + \\sum_{k=1}^{k_n} \\mathbb{E}\\left[Y_{n,k}^2 \\mathbf{1}\\{|Y_{n,k}| > \\varepsilon\\}\\right].'}
      </TeX>

      <p>
        По условию Линдеберга вторая сумма стремится к нулю, поэтому{' '}
        <TeX>{'\\limsup_n \\max_k v_{n,k} \\le \\varepsilon^2'}</TeX>. Так
        как <TeX>{'\\varepsilon > 0'}</TeX> произвольно,
      </p>

      <div className="moments-final">
        <TeX block>
          {'\\boxed{\\;\\max_k v_{n,k} \\longrightarrow 0\\;}'}
        </TeX>
      </div>

      <h3>7. Разложение одной характеристической функции</h3>

      <p>Рассмотрим</p>

      <TeX block>
        {'R(x) = e^{ix} - 1 - ix + \\frac{x^2}{2}, \\qquad e^{ix} = 1 + ix - \\frac{x^2}{2} + R(x).'}
      </TeX>

      <p>
        При <TeX>{'x \\to 0'}</TeX> имеем{' '}
        <TeX>{'R(x) = o(x^2)'}</TeX>, а для всех действительных{' '}
        <TeX>{'x'}</TeX> — глобальная оценка{' '}
        <TeX>{'|R(x)| \\le x^2'}</TeX> (так как{' '}
        <TeX>{'|e^{ix} - 1 - ix| \\le x^2/2'}</TeX>). Подставив{' '}
        <TeX>{'x = t Y_{n,k}'}</TeX> и взяв ожидание с{' '}
        <TeX>{'\\mathbb{E}[Y_{n,k}] = 0'}</TeX>,{' '}
        <TeX>{'\\mathbb{E}[Y_{n,k}^2] = v_{n,k}'}</TeX>, получаем
      </p>

      <TeX block>
        {'\\varphi_{n,k}(t) = 1 - \\frac{t^2 v_{n,k}}{2} + r_{n,k}(t), \\qquad r_{n,k}(t) = \\mathbb{E}[R(t Y_{n,k})].'}
      </TeX>

      <p>
        Теперь нужно доказать, что{' '}
        <TeX>{'\\sum_k |r_{n,k}(t)| \\to 0'}</TeX> — именно здесь
        используется условие Линдеберга.
      </p>

      <h3>8. Контроль суммарного остатка</h3>

      <p>
        Зафиксируем <TeX>{'t \\ne 0'}</TeX> и возьмём произвольное{' '}
        <TeX>{'\\eta > 0'}</TeX>. Так как <TeX>{'R(x) = o(x^2)'}</TeX>,
        существует <TeX>{'\\delta > 0'}</TeX> такое, что{' '}
        <TeX>{'|R(x)| \\le \\eta x^2'}</TeX> при{' '}
        <TeX>{'|x| \\le \\delta'}</TeX>. Разделим область на{' '}
        <TeX>{'|Y_{n,k}| \\le \\delta/|t|'}</TeX> (локальная оценка) и{' '}
        <TeX>{'|Y_{n,k}| > \\delta/|t|'}</TeX> (глобальная оценка):
      </p>

      <TeX block>
        {'|r_{n,k}(t)| \\le \\eta t^2 v_{n,k} + t^2\\,\\mathbb{E}\\left[Y_{n,k}^2 \\mathbf{1}\\{|Y_{n,k}| > \\delta/|t|\\}\\right].'}
      </TeX>

      <p>Суммируя по <TeX>{'k'}</TeX> и используя <TeX>{'\\sum_k v_{n,k} = 1'}</TeX>,</p>

      <TeX block>
        {'\\sum_{k=1}^{k_n} |r_{n,k}(t)| \\le \\eta t^2 + t^2 \\sum_{k=1}^{k_n} \\mathbb{E}\\left[Y_{n,k}^2 \\mathbf{1}\\{|Y_{n,k}| > \\delta/|t|\\}\\right].'}
      </TeX>

      <p>
        По условию Линдеберга при{' '}
        <TeX>{'\\varepsilon = \\delta/|t|'}</TeX> вторая сумма стремится к
        нулю, поэтому{' '}
        <TeX>{'\\limsup_n \\sum_k |r_{n,k}(t)| \\le \\eta t^2'}</TeX>.
        Поскольку <TeX>{'\\eta'}</TeX> произвольно,
      </p>

      <div className="moments-final">
        <TeX block>
          {'\\boxed{\\;\\sum_{k=1}^{k_n} |r_{n,k}(t)| \\longrightarrow 0\\;}'}
        </TeX>
      </div>

      <RemainderControl />

      <h3>9. Сумма отклонений и малость множителей</h3>

      <p>
        Обозначим{' '}
        <TeX>{'z_{n,k}(t) = \\varphi_{n,k}(t) - 1 = -\\tfrac{t^2 v_{n,k}}{2} + r_{n,k}(t)'}</TeX>.
        Тогда
      </p>

      <TeX block>
        {'\\sum_{k=1}^{k_n} z_{n,k}(t) = -\\frac{t^2}{2}\\sum_{k=1}^{k_n} v_{n,k} + \\sum_{k=1}^{k_n} r_{n,k}(t) \\longrightarrow -\\frac{t^2}{2}.'}
      </TeX>

      <p>
        Из неравенства{' '}
        <TeX>{'|z_{n,k}(t)| \\le \\tfrac{t^2}{2} v_{n,k}'}</TeX> и{' '}
        <TeX>{'\\max_k v_{n,k} \\to 0'}</TeX> следует{' '}
        <TeX>{'\\max_k |z_{n,k}(t)| \\to 0'}</TeX>. Кроме того,
      </p>

      <TeX block>
        {'\\sum_{k=1}^{k_n} |z_{n,k}(t)|^2 \\le \\left(\\max_k |z_{n,k}(t)|\\right)\\sum_{k=1}^{k_n} |z_{n,k}(t)| \\longrightarrow 0.'}
      </TeX>

      <h3>10. От произведения к экспоненте</h3>

      <p>
        Так как <TeX>{'\\max_k |z_{n,k}(t)| \\to 0'}</TeX>, для больших{' '}
        <TeX>{'n'}</TeX> все <TeX>{'1 + z_{n,k}(t)'}</TeX> близки к
        единице, и по разложению{' '}
        <TeX>{'\\log(1 + z) = z + O(|z|^2)'}</TeX>
      </p>

      <TeX block>
        {'\\log \\varphi_{S_n}(t) = \\sum_{k=1}^{k_n} z_{n,k}(t) + O\\!\\left(\\sum_{k=1}^{k_n} |z_{n,k}(t)|^2\\right) \\longrightarrow -\\frac{t^2}{2}.'}
      </TeX>

      <p>Возводя экспоненту,</p>

      <div className="moments-final">
        <TeX block>
          {'\\boxed{\\;\\varphi_{S_n}(t) \\longrightarrow e^{-t^2/2}\\;}'}
        </TeX>
      </div>

      <h3>11. Применение теоремы Леви</h3>

      <p>
        Функция <TeX>{'e^{-t^2/2}'}</TeX> — характеристическая функция{' '}
        <TeX>{'Z \\sim N(0,1)'}</TeX>, непрерывная в нуле. По теореме
        непрерывности Леви из поточечной сходимости следует{' '}
        <TeX>{'S_n \\overset{d}{\\longrightarrow} Z'}</TeX>. Возвращаясь к
        исходным величинам,
      </p>

      <div className="moments-final">
        <TeX block>
          {'\\boxed{\\;\\frac{X_{n,1} + \\cdots + X_{n,k_n}}{s_n} \\;\\overset{d}{\\longrightarrow}\\; N(0,1)\\;}'}
        </TeX>
      </div>

      <p>Теорема Линдеберга–Феллера доказана.</p>

      <h3>Почему недостаточно только малости отдельных дисперсий</h3>

      <p>
        Условие <TeX>{'\\max_k v_{n,k} \\to 0'}</TeX> говорит, что ни одна
        отдельная величина не несёт заметной части дисперсии. Но само по
        себе оно не контролирует редкие крупные значения: величина может
        почти всегда быть маленькой, но с малой вероятностью принимать
        огромное значение. Условие Линдеберга требует большего —{' '}
        <TeX>{'\\sum_k \\mathbb{E}[Y_{n,k}^2 \\mathbf{1}\\{|Y_{n,k}| > \\varepsilon\\}] \\to 0'}</TeX>{' '}
        — и напрямую запрещает крупным скачкам нести заметную долю
        дисперсии.
      </p>

      <div className="moments-key">
        Верно, что Линдеберг <TeX>{'\\Rightarrow'}</TeX> Феллер. Обратное
        без дополнительных условий неверно.
      </div>

      <h3>Как классическая ЦПТ следует из теоремы Линдеберга</h3>

      <p>
        Пусть <TeX>{'X_1, X_2, \\ldots'}</TeX> независимы, одинаково
        распределены,{' '}
        <TeX>{'\\mathbb{E}[X_i] = 0'}</TeX>,{' '}
        <TeX>{'\\operatorname{Var}(X_i) = \\sigma^2 < \\infty'}</TeX>. В{' '}
        <TeX>{'n'}</TeX>-й строке возьмём{' '}
        <TeX>{'X_{n,k} = X_k'}</TeX>, тогда{' '}
        <TeX>{'s_n^2 = n\\sigma^2'}</TeX>, и условие Линдеберга
        принимает вид
      </p>

      <TeX block>
        {'\\frac{1}{\\sigma^2}\\,\\mathbb{E}\\left[X_1^2\\,\\mathbf{1}\\{|X_1| > \\varepsilon\\sigma\\sqrt{n}\\}\\right].'}
      </TeX>

      <p>
        При <TeX>{'n \\to \\infty'}</TeX> индикатор стремится к нулю почти
        наверное, а <TeX>{'X_1^2 \\mathbf{1}\\{\\cdots\\} \\le X_1^2'}</TeX>{' '}
        с <TeX>{'\\mathbb{E}[X_1^2] < \\infty'}</TeX>. По теореме Лебега о
        мажорируемой сходимости это выражение стремится к нулю — условие
        Линдеберга выполнено автоматически. После возвращения ненулевого
        среднего получаем классическую ЦПТ:
      </p>

      <div className="moments-final">
        <TeX block>
          {'\\boxed{\\;\\frac{X_1 + \\cdots + X_n - n\\mu}{\\sigma\\sqrt{n}} \\;\\overset{d}{\\longrightarrow}\\; N(0,1)\\;}'}
        </TeX>
      </div>

      <h3>Сравнение с условием Ляпунова</h3>

      <p>
        Условие Ляпунова требует существования момента порядка{' '}
        <TeX>{'2 + \\delta'}</TeX> для некоторого{' '}
        <TeX>{'\\delta > 0'}</TeX>:
      </p>

      <TeX block>
        {'\\frac{1}{s_n^{2+\\delta}} \\sum_{k=1}^{k_n} \\mathbb{E}\\left[|X_{n,k}|^{2+\\delta}\\right] \\longrightarrow 0.'}
      </TeX>

      <p>
        Оно сильнее: на событии{' '}
        <TeX>{'|X_{n,k}| > \\varepsilon s_n'}</TeX> выполнено{' '}
        <TeX>{'X_{n,k}^2 \\le |X_{n,k}|^{2+\\delta} / (\\varepsilon^\\delta s_n^\\delta)'}</TeX>,
        поэтому из условия Ляпунова следует условие Линдеберга. Логика
        такая:
      </p>

      <TeX block>
        {'\\text{Ляпунов} \\;\\Longrightarrow\\; \\text{Линдеберг} \\;\\Longrightarrow\\; \\text{ЦПТ}.'}
      </TeX>

      <h3>Главная идея доказательства</h3>

      <p>
        После нормировки <TeX>{'\\sum_k v_{n,k} = 1'}</TeX>. Условие
        Линдеберга гарантирует, что отдельное слагаемое становится малым
        (<TeX>{'\\max_k v_{n,k} \\to 0'}</TeX>) и что редкие крупные
        значения не дают заметного вклада. Поэтому каждую
        характеристическую функцию можно заменить квадратичным
        приближением{' '}
        <TeX>{'1 - t^2 v_{n,k}/2'}</TeX>, логарифм произведения стремится
        к <TeX>{'-\\tfrac{t^2}{2}\\sum_k v_{n,k} = -\\tfrac{t^2}{2}'}</TeX>,
        и возникает нормальный предел.
      </p>

      <div className="moments-key">
        Если дисперсия суммы создаётся большим числом независимых малых
        вкладов, а редкие крупные скачки не несут заметной части этой
        дисперсии, то нормированная сумма становится приближённо
        нормальной.
      </div>
    </div>
  )
}

export default LindebergProof
